<div class="large-container center-block">
    <h1><%= @article.title %></h1>
    <div>
    <p>Escrito por: <strong><%= @article.user.email %></strong></p>
    <p>Creado en: <strong><%= @article.created_at %></strong></p>
    </div>
    <div>
        <%= @article.content %>
    </div>

    <div>
        <%= link_to "eliminar", @article, method: :delete, class: 'btn btn-outline-danger'%>
    </div>
</div>